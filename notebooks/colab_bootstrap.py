"""Drop this snippet in the first cell of any Colab notebook.

    !pip install -q git+https://github.com/<you>/safety-circuits
    # or, if you've cloned the repo into Drive:
    %cd /content/drive/MyDrive/safety-circuits && pip install -q -e .

Then `import safety_circuits` should work and pick up GPU automatically.
"""
